; VoxPME — cdr_adaptive_odbc.conf (contenu réel de la VM, journal §13.4)
[voxpme-db]
connection = voxpme-db
table = cdr
alias src => appelant
alias dst => appele
alias start => date_heure_debut
alias billsec => duree_secondes
alias disposition => disposition_ast
alias dcontext => dcontext_ast
alias accountcode => accountcode_ast
alias uniqueid => uniqueid_asterisk
