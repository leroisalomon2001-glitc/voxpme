; VoxPME — pjsip.conf bootstrap statique
; Le reste (endpoints, auths, aors, identify Callcentric) est en ARA (Realtime),
; voir ps_endpoints / ps_auths / ps_aors / ps_endpoint_id_ips en base.
; Ce fichier ne contient que ce qui DOIT être statique au démarrage.

; --- Transports ---
[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0

[transport-wss]
type=transport
protocol=wss
bind=0.0.0.0
; NOTE (13.7) : la config TLS pour WSS se fait via http.conf, pas ici.

; --- Trunk Callcentric : registration sortante ---
; Entierement en Realtime depuis la section 13.9 (voir journal J22) :
; endpoint / auth / aor / identify / registration ->
; ps_endpoints / ps_auths / ps_aors / ps_endpoint_id_ips / ps_registrations en base.
; test reload à chaud ajj 11 sep 2026 01:24:07 GMT
